document.addEventListener(DOMContentLoaded, () => { //ejecuta el Script cuando el navegador HTML está completamente cargado y el árbol DOM está construido

    const container = document.querySelector('.container');
    const scoreDisplay = document.getElementById('score');
    const width = 27; // Ancho del board 27 x 27 cuadros de 20px cada uno = 729 cuadros

    const layout = []; //Layout numérico del board














})